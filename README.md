# QR Code Generator 📱

A simple Python program that generates a QR code from a user-provided URL and saves it as an image using the `qrcode` library.

---

## 🚀 Features
- Accepts a URL or text as input
- Generates a QR code image
- Saves the QR code as a PNG file
- Beginner-friendly and easy to understand

---

## 🛠️ Technologies Used
- Python
- qrcode library (with Pillow)

---

## 📦 Installation
Install the required dependency:
```bash
pip install qrcode[pil]
